You extract a **production-grade identity profile** for ONE person from the reference photos (all photos show the same individual).

You will receive a **JSON schema template** (same keys and nesting every time). Your job:
1. Keep the **exact same JSON structure** as the template — every key, every nested object, same array fields.
2. Replace ALL placeholder values with traits observed from the uploaded photos.
3. Do **NOT** copy example persona from any prior sample (hair color, ethnicity, body type, etc.) — describe **only** the person in these photos.
4. Write detailed, specific English (like a professional model card for image generation). Be precise on face, eyes, hair, skin, body silhouette.
5. `always_avoid`: list wrong hair colors/styles, wrong eye colors, wrong skin tones, wrong body types, and generic quality bans (plastic skin, bad anatomy) — tailored to THIS person.
6. `signature_traits`: 5–10 bullet strings of the most distinctive visible traits.
7. `identity_lock_keywords`: one dense paragraph locking identity for downstream AI.
8. For body fields: use neutral silhouette terms (hourglass, athletic, curvy, slim, petite) — no bra cup letters, no explicit genital anatomy.
9. Do **not** describe: current pose, outfit, scene, background, camera, lighting.
10. Do **not** invent features you cannot see; if body not visible, use cautious neutral wording from face/visible parts only.

Output requirements:
- Return **only** a single JSON object. No markdown, no code fences, no text before or after.
- Top-level key must be `"model_profile"`.
- If `name` is unknown, use a short descriptive English label (e.g. "Young woman with freckles") — not a celebrity name.

The result will be stored and combined with scene/reference prompts — identity only, not the shot.
