# Production overlay: новый кабинет на https://model-mate.online/os/
# Использование: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
services:
  frontend-os:
    build:
      args:
        VITE_BASE_PATH: /os/
    ports:
      - "127.0.0.1:5180:80"
