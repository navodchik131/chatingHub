import { VideoStudioPage } from './Video';

/** Seedance 2.0 / 2.5 Sale — тот же UI что «Видео», backend EvoLink, всегда кредиты. */
export default function SeedanceSale() {
  return <VideoStudioPage backend="evolink" />;
}
