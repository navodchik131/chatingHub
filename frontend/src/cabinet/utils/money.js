/** @deprecated Import from `../money/display` — kept for cabinet JS imports. */
export {
  FALLBACK_RUB_PER_USD,
  fetchUsdRate,
  formatCreditOneLiner,
  formatCreditRate,
  formatPlanPrice,
  fillPriceTemplate,
  getCachedRubPerUsd,
} from '../../money/display'

export { formatPlanPrice as formatCreditsPackPrice } from '../../money/display'
